# Camada de serviço: true = mocks em memória, false = HTTP (requer implementação)
VITE_USE_MOCK=true

# FUTURO: URL base do backend FastAPI quando existir
# VITE_API_BASE_URL=https://api.seudominio.com
